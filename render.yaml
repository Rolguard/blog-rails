databases:
  - name: mysite
    databaseName: mysite
    user: mysite
    plan: free

services:
  - type: web
    name: mysite
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        value: postgresql://mysite:n5wEGEoIstT8rS0WiqQ8fOtYOg6a9Xra@dpg-cu86mu2j1k6c739rbe60-a.oregon-postgres.render.com/mysite_9yor
      - key: WEB_CONCURRENCY
        value: 2 # sensible default